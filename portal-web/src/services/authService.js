const TOKEN_KEY='txeka_token',FAILED='txeka_failed_attempts',LOCKOUT='txeka_lockout_until';
const decodeJwt=t=>{try{const b=t.split('.')[1].replace(/-/g,'+').replace(/_/g,'/');return JSON.parse(decodeURIComponent(atob(b).split('').map(c=>'%'+('00'+c.charCodeAt(0).toString(16)).slice(-2)).join('')))}catch{return null}};
export const authService={getToken:()=>localStorage.getItem(TOKEN_KEY),setToken:t=>localStorage.setItem(TOKEN_KEY,t),removeToken:()=>localStorage.removeItem(TOKEN_KEY),logout:()=>{localStorage.removeItem(TOKEN_KEY);window.location.href='/login'},decodeToken:()=>{const t=localStorage.getItem(TOKEN_KEY);return t?decodeJwt(t):null},isAuthenticated:()=>{const p=authService.decodeToken();return p?p.exp>Math.floor(Date.now()/1000):!1},getFailedAttempts:()=>{const r=localStorage.getItem(FAILED);return r?parseInt(r,10):0},incrementFailedAttempts:()=>{const c=authService.getFailedAttempts()+1;localStorage.setItem(FAILED,String(c));return c},resetFailedAttempts:()=>{localStorage.removeItem(FAILED);localStorage.removeItem(LOCKOUT)},getLockoutUntil:()=>{const r=localStorage.getItem(LOCKOUT);return r?parseInt(r,10):0},setLockout:s=>localStorage.setItem(LOCKOUT,String(Date.now()+s*1000)),isLockedOut:()=>{const u=authService.getLockoutUntil();if(!u)return!1;const r=Math.ceil((u-Date.now())/1e3);return r<=0?(authService.resetFailedAttempts(),!1):r},getRemainingLockoutSeconds:()=>{const u=authService.getLockoutUntil();return u?Math.max(0,Math.ceil((u-Date.now())/1e3)):0}};
export default authService;
